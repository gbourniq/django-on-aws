TODOs
- Configure Django app to work with ElastiCache
- email contact form via sns? mock for local
- remove all prints?
- format all files
- update ci/cd pipeline - use everything from makefile basically
- Update README (with screenshots of eg, the dashboard), update architecture diagram?

TODOS
- DynamoDB streams + SNS/SQS queue for email/slack notifications on new posts, and user welcome email
- Implement Lambda worker tier (SAM?) + SQS
- S3 event for notifications on new image / object created? & put metadata to dynamodb?
- Try AWS Cognito for Users management and SSO
- Define/refine Cloudfront caching strategy: Check/update cache based on Headers, Cookies, or Query string parameters
- CodeDeploy blue/green deployment types to replace the whole TG instead of EC2 in_place deployment
- Remove open CidrIPs below - and have Lambda to dynamically update with CF public IPs
# https://aws.amazon.com/blogs/security/automatically-update-security-groups-for-amazon-cloudfront-ip-ranges-using-aws-lambda/

